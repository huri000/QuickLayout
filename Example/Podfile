platform :ios, '9.0'

use_frameworks!

target 'DemoApp' do
  	pod 'QuickLayout', :path => '../'
end

target 'DemoAppTests' do
  	pod 'QuickLayout', :path => '../'
	pod 'Quick', '1.2.0'
    pod 'Nimble', '7.0.2'
end
